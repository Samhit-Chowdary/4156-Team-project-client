package com.nullterminators.project.enums;

public enum PayrollRequestsStatus {
    APPROVED,
    REJECTED,
    PENDING,
    INVALID_FORMAT,
    INVALID_DATA,
    SUCCESS,
    REQUEST_NOT_FOUND,
    ERROR,
    OK,
    EMPLOYEE_NOT_FOUND
}
